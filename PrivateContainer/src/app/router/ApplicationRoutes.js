const PRIVATE_APPLICATION_URL = {
  PROFILE: '/profiles',
  PROFILE_SLUG: '/profiles/:slug',
  PUBLIC_PROFILES_SLUG_APPOINTMENT: '/profiles/:slug/appointment',
};

export { PRIVATE_APPLICATION_URL };
