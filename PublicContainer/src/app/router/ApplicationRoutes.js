const PUBLIC_APPLICATION_URL = {
  PUBLIC: '/',
  PUBLIC_PROFILES: '/profiles',
  PUBLIC_PROFILES_ALL: '/profiles/all',
  PUBLIC_PROFILES_SLUG: '/profiles/:slug',
  PUBLIC_PROFILES_SLUG_APPOINTMENT: '/profiles/:slug/appointment',

  // Auth routes - FE
  AUTH_CONTAINER: '/auth',
  AUTH_SIGNIN_CONTAINER: '/auth/signin',
  AUTH_SIGNUP_CONTAINER: '/auth/signup',
};

export { PUBLIC_APPLICATION_URL };
